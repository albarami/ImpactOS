"""Knowledge Flywheel (MVP-12).

Captures analyst refinements during engagements and promotes validated
patterns into reusable organizational knowledge. All AI components produce
structured JSON only — the deterministic engine performs all computation.
"""
