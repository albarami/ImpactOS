"""Al-Muhasabi Depth Engine — 5-step AI reasoning framework.

Generates structured JSON artifacts that challenge scenarios. NEVER computes
economic results — that is the deterministic engine's job.
"""
